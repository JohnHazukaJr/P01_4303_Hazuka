# Static site lives in `web/` (no install/build).
[build]
  command = "true"
  publish = "web"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
