DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/synodos
JWT_SECRET=change-me
# Comma-separated list of allowed frontend origins for CORS, e.g.
# ALLOWED_ORIGINS=https://synodos-web.onrender.com,https://yourdomain.com
ALLOWED_ORIGINS=
# Set to disable TLS for local Postgres if needed:
# PGSSLMODE=disable

